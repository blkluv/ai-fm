export default function Dashboard() {
  // TODO:
  //  - Button to select a pre-defined radio
  //  - Do a http request and wait until it returns data
  //  - Based on the data, navigate to /radio/$radio_id
  //  - In /radio/$radio_id fetch it again using react-query.
  
  return <div>sdsd</div>
}