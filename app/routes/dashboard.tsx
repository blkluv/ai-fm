import type {RadiosResponse} from "~/types";

export default function Dashboard() {
  // TODO:
  //  - Fetch radio stations from GET http://localhost:5000/radios
  //  - output example (type RadiosResponse): [{"id":"5875bfa8-e9ce-4001-8583-9738d9eefedb","title":"Test Radio Station","description":"Default description","is_public":true,"blockCount":12}]
  //  - List the radio stations (there will not be many, so adjust the design for it).
  //  - When one is clicked, navigate to /radio/$radio_id

  return <div>sdsd</div>
}